service:
  name: sls-seed-react-infra-hosting

provider:
  name: aws
  region: us-east-1
  stage: ${opt:stage, "dev"}

custom:
  hostingBucketName: ${self:service}-${self:provider.stage}-hosting-bucket
  custom: some-random-custom-thingy

resources:
  - ${file(./bucket.yml)}