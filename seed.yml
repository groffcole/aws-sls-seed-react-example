after_deploy:
  - printenv
  - echo "NODE_10_VERSION=${NODE_10_VERSION}" >> user_interface/.env
  - ls -pla user_interface
  - cat user_interface/.env
  - pwd
  - ls -pla
  - ls -pla ../
  - ls -pla ../../
  # - npm --prefix user_interface install user_interface
  # - npm --prefix user_interface run build
  # - aws s3 sync user_interface/build/ s3://sls-seed-react-infra-$SEED_STAGE_NAME-3

# before_remove:
#   - delete the files in the s3 hosting bucket