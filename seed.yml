after_deploy:
  - npm --prefix user_interface install user_interface
  - npm --prefix user_interface run build
  - aws s3 sync user_interface/build/ s3://sls-seed-react-infra-dev

# before_remove:
#   - delete the files in the s3 hosting bucket